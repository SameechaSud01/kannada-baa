// TODO(opposites-game): This screen will render unstyled until OppositeGame's
// NativeWind classes are rewritten to inline styles + tokens. See OppositeGame.tsx.
import OppositeGame from '@/src/games/opposites';

export default function OppositeGameScreen() {
  return <OppositeGame />;
}
